{% extends 'back.html.twig' %}

    {% block body %}




<div class="app-content">
    <div class="side-app">
        <div class="page-header">
            <h4 class="page-title">Liste des produits</h4>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#"><i class="fe   fe-box mr-1"></i> Tables</a></li>
                <li class="breadcrumb-item active" aria-current="page">Data Tables</li>
            </ol>
        </div>
        {% for message in app.flashes('info') %}
            <div class="aler alert-info">
                {{ message }}
            </div>
        {% endfor %}
        <div class="row">
            <div class="col-md-18 col-lg-18">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Liste des produits</div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="example" class="table table-striped table-bordered" >
                                <thead>
                                <tr>
                                    <th class="wd-15p">Id Livraison</th>
                                    <th class="wd-15p">Livreur</th>
                                    <th>Date de Livraison</th>
                                    <th>Etat de Livraison</th>

                                </tr>
                                </thead>
                                <tbody>
                                <tr {% for r in reclamation %}>
                                    <td>{{ r.idreclamation}}</td>
                                    <td>{{ r.idpersonne }}</td>
                                    <td>{{ r.description}}</td>
                                    <td>{{ r.typeReclamation}}</td>



                                    <td><a href="{{ path('uprec',{'id':r.idReclamation}) }}" class="remove">
                                            Update
                                        </a></td>
                                    <td><a href="{{ path('supprec',{'id':r.idReclamation}) }}" class="remove">Delete
                                            <i class="bx bx-trash"></i>
                                        </a></td>
                                </tr {% endfor %}>
                                </tbody>
                            </table>














                            <div class="app-content">
                                <div class="side-app">
                                    <div class="page-header">
                                        <h4 class="page-title"></h4>

                                    </div>
                                    <div id="div_chart"></div>

                                    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
                                    <script type="text/javascript">
                                        gc_draw(piechart, 'div_chart')
                                    </script>
                                </div>
                            </div>


                        </div>

                        <!-- table-wrapper -->




                        <!-- section-wrapper -->
                    </div>
                </div>
            </div>
        </div>
        <script>

            function validateSubmit() {
                result = confirm("Êtes-vous sur de vouloir supprimer l'article ?");
                if (result) {
                    $('#form').submit();


                }
            }
        </script>

        {% endblock %}
















